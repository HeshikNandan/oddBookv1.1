// Meteor.startup(function () {
//   // code to run on server at startup
//   if(Meteor.user() != null){
//     var user = User.findOne({'debit.id': debitId, 'credit.sent': {$exists: true}})
//     Sess
//   }
// });
